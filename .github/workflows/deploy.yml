name: Deploy Vite React App

on:
  push:
    branches:
      - main # или master, если у тебя другая ветка

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔻 Checkout code
        uses: actions/checkout@v4

      - name: 🟨 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: 📦 Install dependencies
        run: npm install

      - name: 🛠 Build (Vite)
        run: npm run build
        env:
          CI: false
          NODE_OPTIONS: --max_old_space_size=4096

      - name: 🔍 Check dist folder exists
        run: |
          if [ -d "dist" ]; then
            echo "✅ dist/ directory exists"
            ls -la dist/
            du -sh dist/
          else
            echo "❌ dist/ directory does NOT exist!"
            exit 1
          fi

      - name: 🔐 Setup SSH key (private)
        run: |
          # Создаём папку
          mkdir -p /home/runner/.ssh
          # Сохраняем ключ (echo -e корректно обрабатывает переносы строк)
          echo -e "${{ secrets.SSH_PRIVATE_KEY }}" > /home/runner/.ssh/id_ed25519
          # Права: только владелец может читать
          chmod 600 /home/runner/.ssh/id_ed25519
          # Диагностика: проверяем, что файл создан и содержит ключ
          echo "🔑 SSH key file created:"
          ls -la /home/runner/.ssh/id_ed25519
          head -n 1 /home/runner/.ssh/id_ed25519
          tail -n 1 /home/runner/.ssh/id_ed25519

      - name: 📂 Copy files to server via SCP
        uses: appleboy/scp-action@v0.1.5
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key_path: /home/runner/.ssh/id_ed25519
          port: ${{ secrets.SSH_PORT }}
          source: "dist"
          target: "/var/www/CRM-TRADER"
          rm: true
          timeout: 30s
          command_timeout: 10m

      - name: 🔁 Reload Nginx
        uses: appleboy/ssh-action@v1.0.2
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key_path: /home/runner/.ssh/id_ed25519
          port: ${{ secrets.SSH_PORT }}
          script: |
            echo "🔄 Reloading nginx..."
            sudo nginx -t && sudo systemctl reload nginx
            echo "✅ Deployment complete! Site is live at http://miss-click.online"
